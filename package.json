{
  "name": "@bastian-paredes/version-manager",
  "version": "1.4.1",
  "type": "module",
  "private": false,
  "scripts": {
    "build": "tsc && rimraf ./dist && node ./esbuild.config.js",
    "bump-version": "npm run build && node ./dist/index.cjs bump-version",
    "bump-version:dry-run": "npm run build && node ./dist/index.cjs bump-version --dry-run --no-git-check --branch=\"develop\" --preid=\"alpha\"",
    "lint": "npx @biomejs/biome check",
    "lint:fix": "npx @biomejs/biome check --write",
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "author": "Bastián Paredes",
  "description": "Command-line tool to easily bump versions, handle pre-releases, and create Git tags for both monorepos and standalone packages.",
  "dependencies": {
    "chalk": "^5.6.0",
    "commander": "^14.0.0",
    "execa": "^9.6.0",
    "prompts": "^2.4.2",
    "semver": "^7.7.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "@types/node": "^24.3.0",
    "@types/package-json": "^5.0.0",
    "@types/prompts": "^2.4.9",
    "@types/semver": "^7.7.0",
    "@vitest/coverage-v8": "^3.2.4",
    "esbuild": "^0.25.9",
    "globals": "^16.3.0",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "type-fest": "^4.41.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "bin": {
    "version-manager": "dist/index.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
